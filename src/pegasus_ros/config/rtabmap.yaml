# ============================================================
#  RTAB-Map SLAM Configuration — Pegasus Disaster Response UAV
#  Sensors: ZED X (RGB-D) + VLP-16 LiDAR + Pixhawk IMU
#
#  v2.3 changes:
#    - Odometry-specific params (Odom/Strategy, Odom/FeatureType, etc.)
#      moved to icp_odometry.yaml and rgbd_odometry.yaml (Fix #3).
#    - Icp/MaxCorrespondenceDistance increased to 1.0 (Fix #5).
#    - Stereo fallback params removed (not used in RGB-D mode).
#
#  This file is loaded by the RTAB-Map SLAM node (mapping +
#  localization) and rtabmap_viz. Odometry nodes load their own
#  dedicated config files instead.
# ============================================================

rtabmap:
  ros__parameters:

    # ── Core SLAM ────────────────────────────────────────────
    Rtabmap/DetectionRate: "1"
    Rtabmap/TimeThr: "700"
    Rtabmap/MemoryThr: "0"
    Rtabmap/LoopThr: "0.11"

    # ── Memory ───────────────────────────────────────────────
    Mem/IncrementalMemory: "true"
    Mem/InitWMWithAllNodes: "false"
    Mem/STMSize: "30"
    Mem/UseOdomGravity: "true"        # Align map Z-axis with gravity via IMU

    # ── ICP LiDAR (used by RTAB-Map for loop closure verification) ──
    Icp/VoxelSize: "0.3"              # Outdoor: 0.3, Indoor: 0.1
    Icp/MaxCorrespondenceDistance: "1.0"   # Fix #5: was 0.15 — must be > VoxelSize
    Icp/PointToPlane: "true"
    Icp/Iterations: "30"
    Icp/MaxTranslation: "1.0"
    Icp/CorrespondenceRatio: "0.01"

    # ── Proximity / Loop Closure ─────────────────────────────
    RGBD/ProximityBySpace: "true"
    RGBD/AngularUpdate: "0.05"
    RGBD/LinearUpdate: "0.1"
    RGBD/LoopClosureIdentityGuess: "true"
    RGBD/NewMapOdomChangeDistance: "0"
    RGBD/OptimizeFromGraphEnd: "false"
    RGBD/OptimizeMaxError: "3.0"

    # ── Graph Optimization ───────────────────────────────────
    Optimizer/Strategy: "1"           # 1=g2o
    Optimizer/Epsilon: "0.00001"
    Optimizer/Iterations: "100"
    Optimizer/Robust: "true"

    # ── 3D Grid / Occupancy Map ──────────────────────────────
    Grid/Sensor: "2"                  # 0=laser, 1=depth camera, 2=both
    Grid/3D: "true"
    Grid/CellSize: "0.05"            # 5 cm cells
    Grid/RangeMax: "30.0"
    Grid/RangeMin: "0.5"
    Grid/MaxObstacleHeight: "2.0"
    Grid/MaxGroundHeight: "0.1"
    Grid/RayTracing: "true"
    Grid/ClusterRadius: "0.1"
    Grid/GroundIsObstacle: "false"

    # ── Visual Features ──────────────────────────────────────
    Kp/MaxFeatures: "500"
    Vis/MaxFeatures: "1000"
    Vis/MinInliers: "15"
    Vis/InlierDistance: "0.1"
    SURF/HessianThreshold: "300"

    # ── Subscription Modes (RTAB-Map SLAM node only) ─────────
    subscribe_rgb: true
    subscribe_depth: true
    subscribe_stereo: false
    subscribe_scan_cloud: true
    subscribe_scan: false

    # ── Sync ─────────────────────────────────────────────────
    approx_sync: true
    approx_sync_max_interval: 0.5
    queue_size: 30
    wait_imu_to_init: false

    # ── IMU ──────────────────────────────────────────────────
    subscribe_imu: true

    # ── Point Cloud Filtering (ZED X range: 0.3–20m) ────────
    cloud_voxel_size: 0.05
    cloud_output_voxelized: true
    cloud_max_depth: 20.0
    cloud_min_depth: 0.3