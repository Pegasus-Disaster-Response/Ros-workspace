# ============================================================
#  RGB-D Visual Odometry Configuration — Pegasus Disaster Response UAV
#  Loaded by: rgbd_odometry node in pegasus_slam.launch.py
#
#  Fix #3: Subscription parameters under the correct node name
#  (rgbd_odometry) so the backup visual odometry node actually
#  subscribes to RGB + depth topics. Previously these lived only
#  in rtabmap.yaml under 'rtabmap:', which is a different namespace.
# ============================================================

rgbd_odometry:
  ros__parameters:

    # ── Subscription Modes ──────────────────────────────────
    subscribe_rgb: true
    subscribe_depth: true
    subscribe_scan_cloud: false
    subscribe_scan: false
    subscribe_stereo: false

    # ── Odometry Strategy ───────────────────────────────────
    Odom/Strategy: "0"                # Frame-to-Map
    Odom/FeatureType: "6"             # GFTT/BRIEF
    Odom/ResetCountdown: "1"
    Odom/ImageDecimation: "1"         # 1=full res, 2=half
    OdomF2M/MaxSize: "2000"

    # ── Sync ────────────────────────────────────────────────
    approx_sync: true
    approx_sync_max_interval: 0.5
    queue_size: 30
    wait_imu_to_init: false