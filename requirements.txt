# Python dependencies for the Pegasus obstacle avoidance stack
# Install with: pip install -r requirements.txt

# Core numerics
numpy>=1.24.0

# Scientific computing (used by A* and integrated planning)
scipy>=1.10.0

# Kalman filtering (existing obstacle tracker)
filterpy>=1.4.5

# Optional: accelerated DBSCAN clustering (falls back to built-in if unavailable)
# scikit-learn>=1.0.0

# Optional: YOLO object detection (camera-based obstacle detection)
# ultralytics>=8.0.0      # YOLOv8  (pip install ultralytics)
# opencv-python>=4.5.0    # camera frame pre-processing

# Optional: RViz/rviz2 marker helpers (visualization only)
# transforms3d>=0.3.1
